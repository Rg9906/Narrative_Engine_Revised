# Implementation Plan: Self-Review Bug Fixes

**Track ID:** self-review-fixes_20260816
**Spec:** [spec.md](./spec.md)
**Created:** 2026-08-16
**Status:** [x] Complete

## Overview

Two `/code-review` passes (one over the night's diff, one over
`src/engines` after a stalled 3-directory attempt), each finding real,
verified bugs, fixed as they were found rather than batched into a
single giant change.

## Phase 1: Fixes from the diff review (bcc7c8c..HEAD)

### Tasks

- [x] Task 1.1: Fixed Story Graph node label bug
      (`src/engines/narrative_graph.py`) — see spec.md #1.
- [x] Task 1.2: Merged duplicate classifier calls
      (`src/memory/theme_memory.py`) — see spec.md #2.

### Verification

- [x] `pytest tests/ -q` — 86 passed.
- [x] Real-data spot check: character node labels ("Mr. Whitmore",
      "Giselle", etc.) confirmed correct via a throwaway script against
      the actual `data/memory/narrative_state.json`.
- [x] `narrative_graph.json` regenerated (413 nodes / 826 edges,
      unchanged counts, only labels differ).

## Phase 2: Fixes from the engines review (src/engines/*.py)

### Tasks

- [x] Task 2.1: Wired `ArcInspector._check_unresolved_arcs` into
      `inspect()` — see spec.md #3. New regression test.
- [x] Task 2.2: Switched 4 builtin-`hash()` call sites in
      `state_engine.py` to `stable_hash()` — see spec.md #4.
- [x] Task 2.3: Resolved relationship-mutation parties through
      `character_memory.resolve_character_id` — see spec.md #5.
- [x] Task 2.4: Added `INTRODUCTION` to the dual-ownership check's gated
      change types in `narrative_state.py::reconcile_state_changes` —
      see spec.md #6.
- [x] Task 2.5: Replaced the bare `except Exception: pass` around scene
      analysis with a logged warning — see spec.md #7.

### Verification

- [x] `pytest tests/test_engines.py -q -k arc` — new regression test
      passes in isolation.
- [x] Full `pytest tests/ -q` re-run after all Phase 2 fixes (host
      machine under heavy, unrelated CPU load tonight — 87% observed —
      making runs slow; correctness confirmed by pass/fail, not timing).

### Final Verification

- [x] All acceptance criteria in spec.md met.
- [x] Full suite green.
- [x] Ready for review / commit.

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
