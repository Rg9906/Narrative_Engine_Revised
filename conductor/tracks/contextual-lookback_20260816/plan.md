# Implementation Plan: Contextual Lookback

**Track ID:** contextual-lookback_20260816
**Spec:** [spec.md](./spec.md)
**Created:** 2026-08-16
**Status:** [x] Complete

## Overview

Single phase: schema + orchestration + retrieval wiring, done together
since each piece is small and they only make sense as a unit.

## Phase 1: Wire previous-chapter excerpt through state, orchestration, and retrieval

### Tasks

- [x] Task 1.1: Added `previous_chapter_excerpt`/`previous_chapter_number`
      fields to `NarrativeState` (`src/models/state.py`), serialized under
      `metadata`.
- [x] Task 1.2: `src/main.py` sets both fields right after
      `state.apply_delta(delta)`, using a new `PREVIOUS_CHAPTER_EXCERPT_CHARS
      = 1500` module constant and `chapter_data.raw_text[-N:].strip()`.
- [x] Task 1.3: `ContextRetriever` (`src/pipeline/context_retriever.py`)
      threads the excerpt from `metadata` through `_extract_sources`/
      `_empty_data_sources`, and `_hydrate_and_compile` renders a new
      `<PreviousChapterExcerpt><Excerpt chapter="N">...</Excerpt></...>`
      tier, included unconditionally (like chapter summaries/timeline)
      when non-empty, omitted otherwise.

### Verification

- [x] New tests in `tests/test_context_retriever.py`: live-state tier
      present with correct content, tier omitted when excerpt is unset,
      and disk-fallback (cold start) path picks it up from `metadata` too.
- [x] New test in `tests/test_engines.py`
      (`test_process_chapter_records_previous_chapter_excerpt`) verifying
      `main.process_chapter` records the *current* chapter's tail for
      later use, using the existing mocked-Pipeline/StateEngine pattern.
- [x] `pytest tests/ -q` — 86 passed (85 baseline + 1; the 3
      context-retriever tests are net +2 after accounting for the
      pre-existing 5).
- [x] Verified against the real, committed `data/memory/narrative_state.json`:
      `NarrativeState.from_dict` loads old-format data gracefully
      (`previous_chapter_excerpt=""`), and a synthetic excerpt set on that
      real state correctly surfaces through a real
      `ContextRetriever.retrieve_context()` call.

### Final Verification

- [x] All acceptance criteria in spec.md met.
- [x] `pytest tests/ -q` passing (86).
- [x] Ready for review / commit.

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
