# Core NLP & text processing
spacy>=3.7,<4.0
gliner>=0.2
fastcoref>=2.1
pymupdf>=1.23
python-docx>=1.1

# Graph & data
networkx>=3.2

# Testing
pytest>=8.0

# Configuration
pyyaml>=6.0

# Utilities
dataclasses-json>=0.6

# Style/emotion analysis — offline, no model download (readability + lexicon sentiment)
textstat>=0.7
vaderSentiment>=3.3

# API server (backs the frontend/ dashboard)
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# === Later phase dependencies (install when needed) ===
# bookNLP  (install via: pip install booknlp)
# sentence-transformers>=2.2

# Zero-shot theme/mystery/symbol classification (src/utils/zero_shot_classifier.py) —
# optional, ~1.6GB model download (facebook/bart-large-mnli). ThemeMemory/MysteryMemory
# fall back to keyword-based detection automatically when these aren't installed.
# transformers>=4.40
# torch>=2.2
